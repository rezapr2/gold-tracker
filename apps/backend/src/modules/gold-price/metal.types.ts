// Deprecated module: the asset model now lives in './asset.types'.
// Re-exported here so existing `metal.types` imports keep working during the
// incremental metal -> asset rename.
export * from './asset.types';
